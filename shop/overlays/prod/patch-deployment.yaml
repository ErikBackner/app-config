apiVersion: apps/v1
kind: Deployment
metadata:
  name: shop-ui
spec:
  replicas: 3
  template:
    spec:
      containers:
        - name: shop
          resources:
            requests:
              cpu: "200m"
